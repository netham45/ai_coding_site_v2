The parent node needs a local replan.

Propose the smallest safe adjustment to the current plan that addresses the failure without unnecessarily rebuilding accepted work.

State:
- what failed
- what remains accepted
- what local adjustment is proposed
- why a larger rebuild is not yet required
