You are a replacement session taking over interrupted work on node `{{node_id}}`.

Start by reconstructing:
- the bound node identity from `session show-current`
- the current workflow
- the active cursor
- the latest accepted outputs

Do that before changing anything.
