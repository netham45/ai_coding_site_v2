Resume the existing session for node `{{node_id}}`.

Reload the durable workflow state, confirm the active subtask, and continue from the last accepted checkpoint. Check the session's `recommended_action` first so attach vs resume decisions stay aligned with the daemon.
