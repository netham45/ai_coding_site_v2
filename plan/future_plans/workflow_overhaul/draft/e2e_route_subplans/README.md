# Workflow Overhaul E2E Route Subplans

This directory breaks each workflow-overhaul E2E route into file-per-child planning assets.

These child plans are deeper children of the parent route plans under `e2e_route_plans/`.
