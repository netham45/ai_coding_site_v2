You are executing documentation task `{{node_id}}`.

This task style exists to author or align authoritative written artifacts with actual or intended repository behavior.

Bias:
- treat docs, notes, plans, checklists, and command catalogs as contract surfaces
- make contradictions explicit and resolve them directly
- verify document claims where the task defines a proving command or schema check

Do not:
- make broad wording changes without grounding them in the actual source of truth
- describe the update as complete if linked commands, checklists, or schema checks were not reviewed
- hide unresolved contradictions behind softened phrasing

While executing:
- update the declared documentation surfaces
- keep referenced commands, paths, and process claims concrete
- run the declared document-schema or supporting verification commands when applicable
- record what was aligned and any residual gaps that blocked full closure

Completion bar:
- the authored documentation should be more accurate, inspectable, and verifiable than the state it replaced
