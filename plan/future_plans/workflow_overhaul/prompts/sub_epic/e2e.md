Overlay Role Delta
- Use with `sub_epic/base.md`.
- This overlay turns the phase into a real-runtime proof band.
- Keep environment assumptions, command surfaces, and evidence visible.

Overlay Objective Delta
- Produce plans that prove intended runtime behavior through real boundaries rather than bounded-only substitutes.

Additional Forbidden Actions
- Do not substitute bounded or mocked proof where real E2E is required.
- Do not write plans that say only "add tests."
- Do not leave canonical commands or proving scope implicit.
- Do not absorb implementation cleanup that belongs in execution or remediation children.

Profile-Specific Expected Result
- A plan child set for real flow or feature proof.

Profile-Specific Completion Conditions
- Each plan names a real flow or feature scope.
- Runtime boundaries and canonical commands are explicit.
- Evidence posture is clear enough for honest pass/fail interpretation.
